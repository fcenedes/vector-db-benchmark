apiVersion: v1
kind: Secret
metadata:
  name: benchmark-secrets
type: Opaque
stringData:
  # Redis configuration
  redis-host: "psc.122222.eu-west1-mz.gcp.cloud.rlrcp.com"
  redis-port: "6739"
  redis-user: "default"
  redis-auth: "REPLACE_WITH_REDIS_PASSWORD"
  # MongoDB configuration
  mongo-host: "cluster0.1234567.mongodb.net"
  mongo-port: "27017"
  mongo-user: "performance"
  mongo-auth: "REPLACE_WITH_MONGO_PASSWORD"
  mongo-read-preference: "primary"
  mongo-write-concern: "1"
  # MongoDB Atlas configuration
  embedding-field-name: "embedding"
  atlas-db-name: "vector-db"
  atlas-collection-name: "vector-collection"
  atlas-vector-search-index-name: "vector-index"
  # Benchmark configuration
  bench-datasets: "gist-960-euclidean"
  bench-engines: "redis"

